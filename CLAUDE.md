# CLAUDE.md — AgriGuard AI

This repo's full agent context lives in **[`AGENTS.md`](AGENTS.md)** — read that file, not this one, for the project brief, current status, tech stack, and non-negotiables.

**Why this file exists separately:** `AGENTS.md` is the emerging cross-agent standard (what IBM Bob's own `/init` generates and reads, and what Codex/Cursor/etc. use too). Claude Code's documented native file is `CLAUDE.md` specifically — as of this writing, whether it also reads `AGENTS.md` automatically is genuinely unsettled (open feature requests, no confirmed native support). Rather than gamble on that, or maintain two files that quietly drift apart, this one just points at the real source so there's exactly one place to keep updated.

If you're Claude Code and can't or don't auto-load `AGENTS.md`: go read it now, then come back to work.
